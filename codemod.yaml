schema_version: "1.0"

name: "next2tanstack"
version: "0.1.0"
description: "Codemod for migrating Next.js to TanStack"
author: "nerdboi008 <mail.nerdboi@gmail.com>"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"


targets:
  languages: ["typescript"]

keywords: ["transformation", "migration"]

registry:
  access: "public"
  visibility: "public"

capabilities: ["fs"]
